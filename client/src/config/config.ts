const config = {
    API_URL: 'https://localhost:7219',
    TOKEN_KEY: 'authToken', // for localStorage
    SIMULATE_MODE: true, // shows simulate/generate buttons in UI
    SIMULATE_PLAYERS_NUM: 10, // how many players will be generated
    SIMULATE_GAMES_LIMIT: 1000, // the max number of games generated per click
};

export default config;
