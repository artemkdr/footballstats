const config = {
    API_URL: '/api',
    TOKEN_KEY: 'authToken', // for localStorage
    SIMULATE_MODE: true,
    SIMULATE_USERS_NUM: 10, // how many users will be generated
    SIMULATE_GAMES_LIMIT: 1000, // the max number of games generated per click
};

export default config;
