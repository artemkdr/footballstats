import { FunctionComponent, ReactElement } from 'react'
import { Text } from '@chakra-ui/react'

export const Team: FunctionComponent = (): ReactElement => {
	return (
		<>			
			<Text>Team.</Text>
		</>
	)
}